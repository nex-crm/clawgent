# WorkOS AuthKit — currently using Magic Auth (email-based passwordless)
# TODO: Re-add Google OAuth when configured in WorkOS Dashboard
WORKOS_CLIENT_ID=client_XXXXXXXXXXXXXXXXX
WORKOS_API_KEY=sk_test_XXXXXXXXXXXXXXXXX
WORKOS_COOKIE_PASSWORD=a-secure-password-at-least-32-characters-long

# Redirect URI — MUST match WorkOS Dashboard allowed redirects
# Development: http://localhost:3001/auth/callback
# Production:  https://clawgent.ai/auth/callback
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3001/auth/callback

# App config
NODE_ENV=development
PORT=3001

# PostHog Analytics
NEXT_PUBLIC_POSTHOG_KEY=phc_XXXXXXXXXXXXXXXXX
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Plivo WhatsApp Integration
# Get credentials from Plivo Console: https://console.plivo.com/dashboard/
PLIVO_AUTH_ID=XXXXXXXXXXXXXXXXX
PLIVO_AUTH_TOKEN=XXXXXXXXXXXXXXXXX
PLIVO_WHATSAPP_NUMBER=15558452872
# Exact webhook URL configured in Plivo Console — used for signature verification.
# Must match exactly (including trailing slash if any). Eliminates proxy header issues.
# Example: https://clawgent.ai/api/whatsapp/webhook
PLIVO_WEBHOOK_URL=https://clawgent.ai/api/whatsapp/webhook
