# WorkOS AuthKit — currently using Magic Auth (email-based passwordless)
# TODO: Re-add Google OAuth when configured in WorkOS Dashboard
WORKOS_CLIENT_ID=client_XXXXXXXXXXXXXXXXX
WORKOS_API_KEY=sk_test_XXXXXXXXXXXXXXXXX
WORKOS_COOKIE_PASSWORD=a-secure-password-at-least-32-characters-long

# Redirect URI — MUST match WorkOS Dashboard allowed redirects
# Development: http://localhost:3001/auth/callback
# Production:  https://clawgent.ai/auth/callback
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3001/auth/callback

# App config
NODE_ENV=development
PORT=3001

# PostHog Analytics
NEXT_PUBLIC_POSTHOG_KEY=phc_XXXXXXXXXXXXXXXXX
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# --- Platform URL ---
# Public-facing base URL (used in messages, allowed origins, dashboard links).
# In production, set this to your domain. In dev, defaults to http://localhost:3001.
# BASE_URL=https://yourdomain.com

# --- Plivo WhatsApp Integration ---
# Get credentials from Plivo Console: https://console.plivo.com/dashboard/
PLIVO_AUTH_ID=XXXXXXXXXXXXXXXXX
PLIVO_AUTH_TOKEN=XXXXXXXXXXXXXXXXX
# REQUIRED: Your Plivo WhatsApp sender number (digits only, no + prefix).
PLIVO_WHATSAPP_NUMBER=
# Exact webhook URL configured in Plivo Console — used for signature verification.
# Must match exactly (including trailing slash if any). Eliminates proxy header issues.
# PLIVO_WEBHOOK_URL=https://yourdomain.com/api/whatsapp/webhook

# --- WhatsApp Branding (optional, defaults shown) ---
# WA_BRAND_HEADER=welcome to clawgent by nex.ai
# WA_BRAND_FOOTER=clawgent.ai
# WA_WELCOME_BODY=yo, i deploy AI agents that actually do stuff.\nmarketing, sales, dev, ops -- pick your agent.\n\nlet's get you set up. takes about 60 seconds.
# WA_HELP_LABEL=clawgent commands

# --- Operational Limits (optional, defaults shown) ---
# MAX_INSTANCES=20
# CONTAINER_MEMORY=1024m
# CONTAINER_MEMORY_SWAP=1024m
# CONTAINER_CPUS=0.5
# WA_RATE_LIMIT_WINDOW_MS=60000
# WA_RATE_LIMIT_MAX=10
# TRUSTED_PROXIES=172.17.0.0/16,127.0.0.1
# ALLOWED_ORIGINS=https://yourdomain.com,http://localhost:3001
